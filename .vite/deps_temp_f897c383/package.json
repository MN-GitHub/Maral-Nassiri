{
  "scripts": {
    "postinstall": "chmod +x ./node_modules/.bin/vite",
    "build": "vite build",
    "start": "vite"
  }
}

  "scripts": {
    "postinstall": "chmod +x ./node_modules/.bin/vite",
    "build": "vite build",
    "start": "vite"
  }
}